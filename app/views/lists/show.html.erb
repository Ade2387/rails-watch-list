<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://cdn.pixabay.com/photo/2016/11/15/07/09/photo-manipulation-1825450_1280.jpg);">
  <div class="container">
    <h1><%= @list.name %></h1>
  </div>
</div>

<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All Movies</h1>
    </div>
    <div class="row">
      <div class="col">
        <p>All <%= @list.movies.count %> movies.</p>
      </div>
    </div>
    <div class="row">
      <% @list.movies.each do |movie| %>
        <div class="col-md-4">
          <div class="movie-card">
            <div class="movie-card-image" style="background-image: url('<%= movie.poster_url %>')"></div>
            <div class="movie-card-content">
              <div class="movie-card-title">
                <h4><%= movie.title %></h4>
              </div>
            </div>
            <div class="movie-card-actions">
              <%# TO DO %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<div class="container">
  <div class="row mt-5">
    <% @list.movies.each do |movie| %>
      <div class="col-4">
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= movie.poster_url %>')">
          <%= movie.title %>
          <%# <%= link_to "+", plant_path(plant), method: :delete, data: { confirm: "Are you sure?" }, class: "card-remove-link" %> %>
        </div>
      </div>
    <% end %>
  </div>
  <div class="row mt-5 justify-content-center">
    <div class="col-4">
      <h4>Add a movie</h4>
      <%# <%= simple_form_for([@list, @bookmark]) do |f| %> %>
        <%# <%= f.input :comment %> %>
        <%# <%= f.input :movie_id, collection: Movie.all, prompt: "Choose a movie" %> %>
        <%# <%= f.submit "Add", class: "btn btn-primary"%> %>
      <%# <% end %> %>
    </div>
  </div>
</div>
